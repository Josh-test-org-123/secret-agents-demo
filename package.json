{
  "name": "secret-agents-demo",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "wrangler": "^4.16.1"
  },
  "scripts": {
    "deploy": "wrangler deploy --config ./wrangler.jsonc"
  }
}