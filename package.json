{
  "name": "secret-agents-demo",
  "version": "1.0.0",
  "description": "A Secret Agents HQ demo application",
  "scripts": {
    "build": "mkdir -p dist && cp public/* dist/ && npx wrangler build",
    "dev": "npx http-server . -p 8080",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "http-server": "^14.1.1"
  },
  "keywords": [
    "secret-agents",
    "demo",
    "cloudflare",
    "workers"
  ],
  "author": "",
  "license": "MIT"
}